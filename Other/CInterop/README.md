# C Interop Practice Project

This project demostrates the ability to use C Interop.

### To Build

1. `cd ziglib && zig build -Drelease-mode=fast`
2. Copy ziglib.dll to `bin/{Debug, Release}/`
3. Run `ConsoleUI.exe`